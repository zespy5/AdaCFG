seed : 10 
device : "cuda"

#dataset
train_data_root : 'unpaired_image_data/day_night/train'
train_latent_data : 'merged_latents_forwards/day_night_train_latents.pt'
train_embedding_data : 'merged_latents_forwards/day-night_train_embeddings.pt'
eval_data_root : 'unpaired_image_data/day_night/valid'
eval_latent_data : 'merged_latents_forwards/day_night_valid_latents.pt'
eval_embedding_data : 'merged_latents_forwards/day-night_valid_embeddings.pt'
data_length : 400
batch_size : 5
learning_rate : 5.0e-7
lr_lambda : 0.8
origin_alpha : 0 

model:
    init_g : 50.0
    divide_out : 0.1
    num_guidance_info : 1
    hidden_dim : 768
    num_layers : 2
    heads : 4
    length : 2

loss:
    negative_prompt: 'ugly, blurry, low resolution, unrealistic, paint, distortion'
    lambda_text : 0.7
    lambda_structure : 0.1
    lambda_mean : 0.3
    lambda_negative : 1.0
    dino_threshold : 0.0
    num_condition : 1
    pnp_injection_rate : 0.6
    clip_ds_use : False
    negative_clip_use : True
    image_size : 512
    gradient : 'decrease'
    schedule_method : 'cosine'


